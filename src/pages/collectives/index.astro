---
const _mode = String(process.env.MODE || '').toLowerCase();
export const prerender = (_mode === 'development' || _mode === 'staging') ? false : ['full', 'collectives'].includes(process.env.SCOPE || 'full');

import Layout from "@layouts/index.astro";
---
<Layout title="Collectives">
    <!-- loop through collectives, add little teasers here -->
</Layout>


