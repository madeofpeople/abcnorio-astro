---
import { Picture } from 'astro:assets';
const { attributes } = Astro.props;
---

<Picture src={attributes.url} width={attributes.width} height={attributes.height} formats={['jpeg', 'webp']} alt={attributes.alt} />